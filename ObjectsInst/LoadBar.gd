extends Node2D

# @onready var res = load("res://Storage.tres")
# @onready var tip = get_node("Sprite2D") 
# @onready var tree
# var added: bool = true
# var id: float
# var gid: float
# var treePath

# # Declare member variables here. Examples:
# # var a = 2
# # var b = "text"


# # Called when the node enters the scene tree for the first time.
# func _ready():
# 	tree = self.get_parent()
# 	added = true
# # Called every frame. 'delta' is the elapsed time since the previous frame.
# func _process(delta):
# 	if added:
# 		pass
# 		#print("up")
# 		self.get_child(3).value =100*((tree.timeLimit - tree.timeCounter)/tree.timeLimit)

